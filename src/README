Requires Python 3.x and g++ .

To build/run :

    $ ./build_lib.sh                # compiles c_calc.c and generates libkorbie.so
    
    $ # the following module install may or may not be necessary on your system.
    $ python3 -m pip install pyopengl pyqtgraph
    
    $ python3 korbie.py
