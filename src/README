Requires Python 3.x and g++ .

To build/run :

    $ ./build_lib.sh                # compiles c_calc.c and generates libkorbie.so
    
    $ # the following module installation may or may not be necessary on your system.
    $ python3 -m pip install pyopengl pyqtgraph --user
    
    $ python3 korbie.py
